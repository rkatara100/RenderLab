/**
 * Phase 0 scaffolding placeholder. The real dashboard shell — route
 * structure, dark/light mode, and loading/empty/error/offline states for
 * every data-fetching view — is Phase 3 work (ARCHITECTURE.md sections 2, 7).
 */
export default function HomePage(): React.JSX.Element {
  return (
    <main>
      <h1>RenderLab</h1>
      <p>Dashboard scaffold — Phase 3 will build the real shell here.</p>
    </main>
  );
}
