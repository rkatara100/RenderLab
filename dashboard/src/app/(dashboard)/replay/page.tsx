import { ComingSoon } from '../../../components/shared/ComingSoon';

export default function ReplayPage(): React.JSX.Element {
  return <ComingSoon title="Replay" phase="Phase 7 (session recording & replay)" />;
}
